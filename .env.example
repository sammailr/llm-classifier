# This is a root-level .env.example
# Each service has its own .env file in its directory

# See api/.env.example for API configuration
# See worker/.env.example for Worker configuration
# See frontend/.env.example for Frontend configuration
